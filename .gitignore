# Claude-drafted material kept for personal study only — must never be
# part of the submission or the pushed repo.
_ai_reference_DO_NOT_SUBMIT/

# Local dev scratch (generated keys, local gateway data/logs) — not part
# of the task; the real signing keys are generated at Docker build time.
local-dev/

# Editor/tooling noise
.history/
.claude/
node_modules/
*.duckdb
package-lock.json
